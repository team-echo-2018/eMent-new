/* Skill Entity */
var Skill = function (skillId, skillName, skillLevel, skillCategory) {

    this.skillId = skillId;
    this.skillName = skillName;
    this.skillLevel = skillLevel;
    this.skillCategory = skillCategory;
}

module.exports = Skill;
